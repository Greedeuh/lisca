## CLI

## Link hook

When reading a message, optionnally pass a href link which appear in the queue entry. On click follow the link.

Use case: ai agent use the CLI to send you messages, on click go to agent session